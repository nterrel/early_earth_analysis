## Comparing nvidia and original pytorch neighbor list timings

Note that the nv version is slow on HPG specifically for some reason -- fast on local cluster but hangs a lot with importing libraries. 

